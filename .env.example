# Optional: enables real LLM-based Natural Language -> SPARQL.
GROQ_API_KEY=

# Optional: when your teammate exposes the RDF graph through Fuseki/Virtuoso/etc,
# paste the SPARQL query endpoint here. If empty, the website still works using
# the supplied 50-sign CSV as the local result layer.
SPARQL_ENDPOINT_URL=
